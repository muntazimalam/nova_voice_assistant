fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
jinja2>=3.1.4
pydantic-settings>=2.4.0
numpy>=1.26.0
google-genai>=1.0.0
faster-whisper>=1.0.0
edge-tts>=6.1.0
openwakeword>=0.5.0
onnxruntime>=1.19.0
httpx>=0.27.0

# XTTS-v2 (TTS_ENGINE=xtts) — human-sounding local neural voice.
# CPU wheels (the +cpu build) come from the PyTorch CPU index; the two
# transformer pins matter on Python 3.13: 5.x dropped isin_mps_friendly,
# <4.57 lacks is_torchcodec_available.
--extra-index-url https://download.pytorch.org/whl/cpu
coqui-tts==0.27.5
torch==2.14.0+cpu
torchaudio==2.11.0+cpu
torchcodec==0.16.0+cpu
transformers>=4.57,<4.60